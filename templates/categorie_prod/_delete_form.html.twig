<form method="post" action="{{ path('app_categorie_prod_delete', {'id': categorie_prod.idCategorie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie_prod.idCategorie) }}">
    <button class="btn">Delete</button>
</form>
